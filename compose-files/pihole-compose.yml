name: pihole

# More info at https://github.com/pi-hole/docker-pi-hole/ and https://docs.pi-hole.net/
services:
  pihole:
    container_name: pihole
    image: pihole/pihole:latest
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    #  - "${PIHOLE_PORT_HTTP:-}:80/tcp"
    #  - "${PIHOLE_PORT_HTTPS:-}:443/tcp"
      #- "67:67/udp"      # to use Pi-hole as DHCP server
      #- "123:123/udp"    # to use Pi-hole as NTP server
    environment:
      TZ: ${TZ:-Europe/London}
      # Set a password to access the web interface. Not setting one will result in a random password being assigned
      FTLCONF_webserver_api_password: ${PIHOLE_WEBSERVER_API_PASSWORD:?required}
      # If using Docker's default `bridge` network setting the dns listening mode should be set to 'ALL'
      FTLCONF_dns_listeningMode: 'ALL'
    volumes:
      - ${SERVER_DATA_FOLDER:-/root/labdata}/pihole:/etc/pihole
      #- './etc-dnsmasq.d:/etc/dnsmasq.d'    # Uncomment if you have custom dnsmasq config file for migration and can be deleted afterwards. Needs environment variable FTLCONF_misc_etc_dnsmasq_d: 'true'
    #cap_add:
      # See https://docs.pi-hole.net/docker/#note-on-capabilities
      # Required if you are using Pi-hole as your DHCP server, else not needed
      #- NET_ADMIN
      # Required if you are using Pi-hole as your NTP client to be able to set the host's system time
      #- SYS_TIME
      # Optional, if Pi-hole should get some more processing time
      #- SYS_NICE
    restart: unless-stopped
    networks:
      - app-net

networks:
  app-net:
    name: ${NETWORK_NAME:-pihole-bridge}
